# Block npm from walking up the directory tree and discovering the parent
# pnpm-workspace, which makes `npm install` refuse `workspace:*` specifiers.
# Vercel runs the install in apps/preview; this is the only node_modules npm
# should see.
install-strategy=nested
fund=false
audit=false
workspaces=false
legacy-peer-deps=true
engine-strict=false
